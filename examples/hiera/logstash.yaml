# Apply to logstash indexers
---
  stack_logstash::logstash_install: true

  # Usage sample for a custom logstash.conf template (from a custom site module)
  # stack_logstash::logstash_config_template: 'site/stack/logstash/logstash.conf.erb'
