/* File managed by Puppet */

var config = new Settings(
{
  elasticsearch:    "<%= scope.lookupvar('stack_logstash::elasticsearch_protocol') %>://<%= scope.lookupvar('stack_logstash::real_elasticsearch_server') %>:<%= scope.lookupvar('stack_logstash::elasticsearch_server_port') %>",
  kibana_index:     "kibana-int",
  modules:          ['histogram','map','pie','table','filtering',
                    'timepicker','text','fields','hits','dashcontrol',
                    'column','derivequeries','trends','bettermap','query',
                    'terms'],
  }
);

